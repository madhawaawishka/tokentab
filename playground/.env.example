# Copy this file to `.env` and paste your keys. Either provider works on its own.
# (playground/.env is gitignored via the repo's .tokenmeter/ + *.log rules — keep keys out of git.)

# Groq — https://console.groq.com/keys
GROQ_API_KEY=

# Google Gemini — https://aistudio.google.com/apikey
GEMINI_API_KEY=
